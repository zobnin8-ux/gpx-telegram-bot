version: "3.9"
services:
  gpx-bot:
    build: .
    container_name: gpx-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./public/maps:/app/public/maps
      - ./data/uploads:/app/data/uploads
